use err::RuwmError;
/*
 * Unlike i3, we will not be using a custom parser
 * Instead we will be using a PEG (Parsing Expression Grammer) 
 * parser
 *
 */

pub struct ConfigParser;

impl ConfigParser {
	pub fn new() -> Result<ConfigParser, RuwmErro> {

	}	

} 